{
  "name": "basic1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "ts-node src/main.ts",
    "build": "tsc",
    "clean": "rimraf build",
    "build:run": "npm run build && node build/main.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "ts-node": "^10.9.2",
    "rimraf": "^5.0.0"
  }
}
