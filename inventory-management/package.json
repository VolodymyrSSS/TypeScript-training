{
  "name": "inventory-management",
  "version": "1.0.0",
  "main": "build/main.js",
  "scripts": {
    "start": "ts-node src/main.ts",
    "build": "tsc",
    "build:run": "npm run build && node build/main.js",
    "clean": "rimraf build"
  }
}
